# https://numtide.github.io/devshell
[[commands]]
package = "devshell.cli"
help = "Per project developer environments"

[[commands]]
help = "Rebuilds nixos system."
name = "rebuild"
command = "sudo nixos-rebuild switch --flake flake.nix #.$HOSTNAME"
